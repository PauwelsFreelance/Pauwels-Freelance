# NEVER commit the mailbox password
config.php

# vendor/ IS committed on purpose — see GIT-SETUP.md. Cesky Hosting can't
# run Composer itself, and committing vendor/ keeps every deploy a plain,
# ordinary fast-forward instead of a synthetic build commit.
composer.phar

# OS junk
.DS_Store
Thumbs.db
